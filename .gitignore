# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
# NOTE: `_sass/vendors` and `assets/js/dist` are intentionally tracked here,
# unlike upstream Chirpy, since the site builds from this repo's theme sources.

# Hidden system files
*~
.DS_Store
Thumbs.db
